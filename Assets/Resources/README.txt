This folder contains resources that we need to load at runtime 
(e.g. Resources.Load()). If there are any resources you need to 
load via script, put them in here and use Resources.Load().

That is all